Обновить конфиг файл clangd: compiledb make

include <stdint.h> - подключение имен типов (size_t)
2> text.txt вывод в text.txt потока stderr
NAN +-* val = NAN
isnormal - проверка на нормальное число
gitlog - история комитов
git checkout <хэш комита>
git ammend -
man7
ctrl shift p -> wsl
$(filter-out $(SRC_DIR)/experiments.cpp, $(wildcard $(SRC_DIR)/*.cpp))
[[maybe_unused]]
numpad: alt 0151 - срднее тире
strerror, perror

doxygen
/*
    Копирует содержимое из исходной области памяти в целевую область память
    @param[out] dest Целевая область памяти
    \param[in] src Исходная область памяти
    \param[in] n Количество байтов, которые необходимо скопировать
    \return Сумму двух чисел, переданных в качестве аргументов
    \brief
    \warning - предупреждение о влиянии функции
    \example - пример вызова функции

*/

#include <cmath> имеет простарнство имен
\

// #define FREE(ptr)                \
//     ({                           \
//         typeof(ptr) ptr__ = ptr; \
//         free(ptr);               \
//         ptr = NULL;              \
//         ptr__                    \
//     })



@./a.out - не выводит stderr

make

make debug

